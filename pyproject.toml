[project]
name = "curl-fixtures"
version = "0.1.0"
description = "Live-validated curl fixtures for LLM provider APIs"
requires-python = ">=3.10"
dependencies = [
    "PyYAML>=6.0,<7",
]

[tool.uv]
package = false
